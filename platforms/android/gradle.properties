org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Maven publication version for the :unitrack, :unitrack-firebase and
# :unitrack-snowplow modules. JitPack overrides this with -PVERSION_NAME=<tag>
# at build time (see jitpack.yml). Local builds pick up this default — keep in
# sync with root CHANGELOG.
VERSION_NAME=1.0.0
