name: unitrack_firebase_example
description: Minimal Firebase provider sample for UniTrack.
publish_to: none
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: '>=2.0.0 <5.0.0'
  unitrack:
    path: ../../
  unitrack_firebase:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
